// import ArticleList from './ArticleList';

type PostMeta = {
  title: string;
  date: string;
  category: string;
  slug: string;
  thumbnail?: string;
  excerpt: string;
};

// ダミーデータ完全削除済み

export default function ArticleListTest() {
  return null;
}
